# Allow access to images only
<FilesMatch "\.(jpg|jpeg|png|gif|webp)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>

# Deny access to PHP files for security
<FilesMatch "\.php$">
    Order Deny,Allow
    Deny from all
</FilesMatch>

